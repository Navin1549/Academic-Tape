import React from "react";
import Base from "../components/Base";

const About = ()=>{
    return(
        <Base>
            <p>this is a about page </p>
        </Base>
    );
};

export default About;